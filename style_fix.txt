  <style>
    /* ... (previous styles) ... */
    .carousel-section { 
      padding: 60px 0; 
      position: relative; 
      display: flex; 
      flex-direction: column; 
      align-items: center;
    }
    .carousel-outer {
      width: 100%;
      height: 480px; /* Space for scaled cards */
      display: flex;
      align-items: center;
      overflow: hidden;
      position: relative;
    }
    .carousel-track {
      display: flex;
      flex-direction: row;
      flex-wrap: nowrap;
      gap: 32px;
      align-items: center;
      position: absolute;
      left: 0;
      transition: transform 0.6s cubic-bezier(0.23, 1, 0.32, 1);
      padding: 0 50vw; 
    }
    .carousel-controls { 
      display: flex; 
      align-items: center; 
      justify-content: center; 
      gap: 32px; 
      margin-top: 40px; /* Gap between cards and controls */
      width: 100%;
    }
  </style>
